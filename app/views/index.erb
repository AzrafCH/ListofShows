<h3>Here are the results of your search: </h3>
<% @shows.collect do |show| %>
<ol>
  <li><input type="checkbox" name="list[show_ids][]" id = "show[:id]" value = "show[:id]"><%=show.show_url%></li>
</ol>
  <% end %>
  <br>

  <h2>Create New List</h2>
  <form action = "/list" method = "POST">
    <input type = "text" name = "list[name]" placeholder="List Name"><br>
    <input type = "submit">
  </form>
