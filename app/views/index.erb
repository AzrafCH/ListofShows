

<h3>Here are the results of your search: </h3>
<% @shows.collect do |show| %>
<ol>
   <li><input type="checkbox" name= "params[show_id]" id = "<%=show.id%>" value = "<%=show.id%>"><%= " #{show.name} | #{show.overview}" %></li>
</ol>
  <% end %>
 <br>

<%# <% @shows.each do |show| %>
<%#   <ol>
  <input type="checkbox" name="list[show_ids][]"><li><%= show.first%><%# </li>
<%#   </ol>S
  <% end %>

  <h2>Create New List</h2>
  <form action = "/list/new" method = "POST">
    <input type = "text" name = "params[title]" placeholder="List Name"><br>
    <input type = "submit" value = "Create List">
  </form>

  <%#<h2>Add to Existing List</h2>
